# Shell scripts must keep LF endings — they run in Linux containers, and CRLF
# would break the shebang / argument parsing (e.g. docker-entrypoint.sh).
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
